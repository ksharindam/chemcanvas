#!/usr/bin/make -f

PACKAGE = chemcanvas
DESTDIR = $(CURDIR)/debian/$(PACKAGE)

#export PYBUILD_INSTALL_ARGS=--install-lib=/usr/share/$(PACKAGE)/

%:
	dh $@ --buildsystem=pybuild --with=python3


#override_dh_auto_install:
#	$(MAKE) install DESTDIR=`pwd`/debian/chemcanvas


